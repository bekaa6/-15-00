# Lab_12_Abstract_Classes

## 📚 1. Жұмыстың Тақырыбы және Мақсаты

**Тақырыбы:** Объектіге бағытталған бағдарламалаудағы абстрактілі кластар мен интерфейстер

**Мақсаты:** Python-ның `abc` модулін қолдана отырып, объектіге бағытталған бағдарламалаудағы (ОББ) **абстракцияны** зерттеу және қолдану. Подкластардағы мінез-құлық келісімшарттарын орындауды және масштабталатын ОББ архитектураларын жобалауды үйрену.

**Орындаған:** [Бисенбаев Бекарыс]
**Топ:** [ИС 23-14

---

## 2. Теориялық Негіз

Абстрактілі класс — бұл болашақ подкластар үшін үлгіні анықтайтын класс. Ол толығымен іске асырылған әдістерді де, іске асырылмаған (абстрактілі) әдістерді де қамтуы мүмкін.

* **`ABC`:** Барлық абстрактілі кластар үшін негізгі класс.
* **`@abstractmethod`:** Подкластар орындауға міндетті әдістерді белгілейтін декоратор.
* **Интерфейс:** Python-да интерфейс әдетте тек абстрактілі әдістерден тұратын абстрактілі класспен ұсынылады.

---

## 3. Тапсырмалар және Нәтижелер

Әрбір тапсырма үшін кодты `screenshots/` қалтасындағы тиісті скриншоттармен бірге ұсынамын.

### ➡️ Task 1: Алғашқы Абстрактілі Класты Құру (`task1_vehicle.py`)

* **Сипаттама:** `Vehicle` абстрактілі класын `move()` және `fuel_type()` абстрактілі әдістерімен құру. Оларды `Car` және `Bicycle` подкластарында іске асыру.
* **Көрсетілетін принцип:** Абстрактілі әдістерді орындауды қамтамасыз ету.
* **Нәтиже:**
    ```
    --- Car (Автокөлік) тестілеу ---
    Автокөлік жолда жүріп келеді.
    Отын түрі: Бензин/Мұнай.
    ...
    ```
    

### ➡️ Task 2: Нақты Интерфейсті Іске Асыру (`task2_database.py`)

* **Сипаттама:** `connect()`, `execute_query()`, `close()` абстрактілі әдістері бар `DatabaseInterface` интерфейс тәрізді класын құру. `SQLiteDatabase` және `PostgreSQLDatabase` подкластарында модельденген іске асыруларды ұсыну.
* **Көрсетілетін принцип:** Жүйелік компоненттер үшін бірыңғай API келісімшартын анықтау.
* **Нәтиже:**
    ```
    --- Testing SQLiteDatabase ---
    Connecting to SQLite...
    Executing SQLite query: SELECT * FROM users;
    ...
    ```
    

### ➡️ Task 3: Жартылай Абстракция (`task3_logger.py`)

* **Сипаттама:** `header()` нақты әдісі және `log(message)` абстрактілі әдісі бар `Logger` класын құру. `FileLogger` (файлға жазады) және `ConsoleLogger` (экранға шығарады) арқылы әртүрлі іске асыру.
* **Көрсетілетін принцип:** Абстрактілі класта қайта пайдалануға болатын (header) және міндетті (log) логиканы біріктіру.
* **Нәтиже:**
    ```
    --- Testing ConsoleLogger ---
    === LOG START ===
    [CONSOLE LOG]: Application started successfully.
    ...
    ```
    

### ➡️ Task 4: Абстрактілі Кластармен Полиморфизм (`task4_polymorphism.py`)

* **Сипаттама:** Әртүрлі көліктердің (`Car`, `Bicycle`, `Airplane`, `Train`) тізімін құру және тізімді айналып өтіп, барлығына бірдей `move()` әдісін шақыру.
* **Көрсетілетін принцип:** Бір әдіс шақыруының объектінің нақты типіне байланысты әртүрлі мінез-құлықты тудыруы.
* **Нәтиже:**
    ```
    --- Demonstrating Polymorphic Behavior ---
    Car: Жолда жүруде.
    Bicycle: Адам күшімен педаль арқылы қозғалуда.
    Airplane: Аспанда ұшуда.
    Train: Рельстерде жүруде.
    ```
    

### ➡️ Task 5: Төлем Жүйесіне Арналған Интерфейсті Жобалау (`task5_payment_system.py`)

* **Сипаттама:** `authorize()`, `process()`, `refund()` әдістері бар `PaymentSystem` абстрактілі интерфейсін жобалау. Оны `PayPal`, `KaspiPay` және `VisaPayment` подкластарында әртүрлі логикамен іске асыру.
* **Көрсетілетін принцип:** Нақты әлемдегі жүйелерде (төлем шлюздерінде) интерфейсті қолдану.
* **Нәтиже:**
    ```
    --- Тестілеу: PayPal ---
    PayPal: Авторизация сұрауы $55.99 сомасына жіберілді.
    -> Өңдеу нәтижесі: Транзакция PayPal арқылы сәтті аяқталды.
    ...
    ```
    

---

## 4. Қорытынды

Бұл зертханалық жұмыс барысында мен Python-дағы абстрактілі кластарды (`abc` модулі) тиімді қолдануды үйрендім.

* Абстрактілі кластар **келісімшарттарды** қалай мәжбүрлейтінін және архитектураның тұтастығын қалай қамтамасыз ететінін түсіндім.
* **Полиморфизм** принципін іс жүзінде қолдану, бір әдісті әртүрлі объектілерде қауіпсіз шақыру мүмкіндігін көрсетті.
* Нақты әдістер мен абстрактілі әдістерді біріктіретін **жартылай абстракция** (`Logger`) арқылы кодтық қайта пайдалануды қалай арттыруға болатынын анықтадым.